name: Build WLED-MM 14.7.1 32EQ ESP32-WROOM32

on:
  push:
    branches:
      - wled-mm-14.7.1-32eq
  workflow_dispatch:

jobs:
  build-32eq:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install PlatformIO
        run: pip install -r requirements.txt

      - name: Apply 32-band GEQ patch
        run: python3 tools/wled-mm-14.7.1-32eq-patcher.py .

      - name: Make GEQ direct 32-column display
        run: python3 tools/patch-geq-direct.py .

      - name: Build ESP32-WROOM-32 4MB V4
        env:
          WLED_RELEASE: True
        run: pio run -e esp32_4MB_V4_M

      - name: Collect firmware
        run: cp .pio/build/esp32_4MB_V4_M/firmware.bin WLEDMM_14.7.1_ESP32-WROOM32_32EQ-Direct.bin

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: WLEDMM_14.7.1_ESP32-WROOM32_32EQ-Direct
          path: WLEDMM_14.7.1_ESP32-WROOM32_32EQ-Direct.bin
